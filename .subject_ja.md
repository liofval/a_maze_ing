# A-Maze-ing

**This is the way**

*概要: 独自の迷路ジェネレーターを作成し、その結果を表示せよ！*

*バージョン: 2.1*

---

## 目次

- [I - まえがき](#i---まえがき)
- [II - AI に関する指示](#ii---ai-に関する指示)
- [III - 共通指示](#iii---共通指示)
- [IV - 必須パート](#iv---必須パート)
- [V - 視覚的表現](#v---視覚的表現)
- [VI - コード再利用性の要件](#vi---コード再利用性の要件)
- [VII - README の要件](#vii---readme-の要件)
- [VIII - ボーナス](#viii---ボーナス)
- [IX - 提出とピアレビュー](#ix---提出とピアレビュー)

---

## I - まえがき

迷路は何千年もの間、人類を魅了してきた。ギリシャ神話における伝説の**クノッソスの迷宮** ── ミノタウロスを閉じ込めるためにダイダロスが建造した ── から、現代のパズル本やビデオゲームに至るまで、迷路は常に謎、挑戦、そして巧みなデザインの象徴であった。コンピュータサイエンスにおいて、迷路生成は単なる遊びではなく、アルゴリズム、乱数性、グラフ理論の実践的な応用である。迷路生成に使われる有名なアルゴリズム ── *Prim* のアルゴリズム、*Kruskal* のアルゴリズム、*再帰バックトラッカー*など ── は、ネットワーク設計やプロシージャルコンテンツ生成といった実世界の問題にも使われている。興味深いことに、完全迷路（任意の2点間に唯一の経路が存在する迷路）は、グラフ理論における*全域木*と直接関連している。迷路を構築し、特にそれを視覚化して共有できるようにすることは、コンピュータがカオスから構造を生み出す仕組みを探求する素晴らしい方法であり、そしてその過程を楽しむことができる。

> *「迷宮とは迷う場所ではなく、道を見つける場所である。」*
>
> ── 作者不明

---

## II - AI に関する指示

### コンテキスト

学習の過程で、AIはさまざまなタスクを支援することができる。AIツールの多様な機能と、それがあなたの作業をどのようにサポートできるかを探る時間を取ること。ただし、常に慎重にアプローチし、結果を批判的に評価すること。コード、ドキュメント、アイデア、技術的な説明であれ、質問が適切であったか、生成されたコンテンツが正確であるかを完全に確信することはできない。ピアは、ミスや盲点を避けるための貴重なリソースである。

### 主なメッセージ

- 反復的または退屈なタスクを減らすためにAIを使用すること。
- コーディングおよび非コーディングの両方のプロンプティングスキルを身につけること ── これは将来のキャリアに役立つ。
- AIシステムの動作を学び、一般的なリスク、バイアス、倫理的問題をより適切に予測し回避すること。
- ピアと協力して、技術的スキルとソフトスキルの両方を構築し続けること。
- 完全に理解し、責任を取れるAI生成コンテンツのみを使用すること。

### 学習者のルール

- AIツールを探索し、その仕組みを理解する時間を取ること。これにより倫理的に使用し、潜在的なバイアスを減らすことができる。
- プロンプトを書く前に問題を振り返ること ── これにより、正確な語彙を使って、より明確で詳細で関連性の高いプロンプトを書くことができる。
- AIが生成したものすべてを体系的にチェック、レビュー、質問、テストする習慣を身につけること。
- 常にピアレビューを求めること ── 自分自身の検証だけに頼らないこと。

### フェーズの成果

- 汎用的およびドメイン固有のプロンプティングスキルを開発する。
- AIツールの効果的な使用により生産性を向上させる。
- 計算的思考、問題解決、適応力、コラボレーションを引き続き強化する。

### コメントと例

- 試験、評価などで、本当の理解を示さなければならない場面に定期的に遭遇する。準備を怠らず、技術的スキルと対人スキルの両方を磨き続けること。
- ピアと推論や議論を行うことで、自分の理解のギャップが明らかになることが多い。ピアラーニングを優先すること。
- AIツールは特定の文脈を欠くことが多く、一般的な回答を提供しがちである。同じ環境を共有するピアは、より関連性が高く正確な洞察を提供できる。
- AIが最も可能性の高い回答を生成するのに対し、ピアは代替的な視点や貴重なニュアンスを提供できる。品質チェックポイントとして頼ること。

**良い例:**
> AIに「ソート関数をどうテストすればいい？」と聞く。いくつかのアイデアをもらい、試してみる。ピアと結果をレビューし、一緒にアプローチを改善する。

**悪い例:**
> AIに関数全体を書かせ、プロジェクトにコピペする。ピアレビューで何をしているか、なぜそうなっているか説明できず、信頼性を失い、プロジェクトに落ちる。

**良い例:**
> AIを使ってパーサーの設計を手伝ってもらう。その後、ピアとロジックを一通り確認する。バグを2つ見つけ、一緒に書き直す ── より良く、クリーンに、完全に理解した状態で。

**悪い例:**
> Copilotにプロジェクトの重要な部分のコードを生成させる。コンパイルは通るが、パイプの処理方法を説明できない。評価で正当化できず、プロジェクトに落ちる。

---

## III - 共通指示

### III.1 一般ルール

- プロジェクトは **Python 3.10以降**で記述すること。
- プロジェクトは **flake8** コーディング規約に従うこと。
- 関数はクラッシュを避けるために例外を適切に処理すること。潜在的なエラーを管理するために `try-except` ブロックを使用すること。ファイルやコネクションなどのリソースには、自動クリーンアップのためにコンテキストマネージャーを優先すること。レビュー中にハンドルされていない例外でプログラムがクラッシュした場合、機能していないとみなされる。
- すべてのリソース（ファイルハンドル、ネットワークコネクションなど）は、リークを防ぐために適切に管理すること。可能な限りコンテキストマネージャーを使用すること。
- コードには、関数パラメータ、戻り値の型、および適用可能な変数に型ヒント（`typing`モジュールを使用）を含めること。静的型チェックには **mypy** を使用すること。すべての関数はmypyをエラーなく通過すること。
- PEP 257に従い（Google または NumPy スタイルなど）、関数やクラスにdocstringを含めて、目的、パラメータ、戻り値を文書化すること。

### III.2 Makefile

プロジェクトに `Makefile` を含めて一般的なタスクを自動化すること。以下のルールを含む必要がある（必須の lint は指定フラグを暗黙に含む。拡張チェック用に `-strict` を試すことが強く推奨される）：

- **install**: `pip`、`uv`、`pipx`、またはその他任意のパッケージマネージャーを使用してプロジェクト依存関係をインストール。
- **run**: プロジェクトのメインスクリプトを実行（例：Pythonインタプリタ経由）。
- **debug**: Pythonの組み込みデバッガー（例：pdb）を使用してデバッグモードでメインスクリプトを実行。
- **clean**: 一時ファイルやキャッシュ（例：`__pycache__`、`.mypy_cache`）を削除してプロジェクト環境をクリーンに保つ。
- **lint**: `flake8 .` および `mypy . --warn-return-any --warn-unused-ignores --ignore-missing-imports --disallow-untyped-defs --check-untyped-defs` コマンドを実行。
- **lint-strict**（オプション）: `flake8 .` および `mypy . --strict` コマンドを実行。

### III.3 追加ガイドライン

- プロジェクトの機能を検証するテストプログラムを作成すること（提出・採点対象外）。`pytest` や `unittest` などのフレームワークを使用して、エッジケースをカバーする単体テストを行うこと。
- Pythonアーティファクトを除外する `.gitignore` ファイルを含めること。
- 開発時の依存関係の分離のために、仮想環境（例：venv、conda）の使用を推奨する。

*プロジェクト固有の追加要件がある場合は、このセクションの直後に記載される。*

---

## IV - 必須パート

### IV.1 概要

Pythonで迷路ジェネレーターを実装する。設定ファイルを受け取り、迷路（入口と出口の間に唯一の経路を持つ完全迷路も可能）を生成し、16進数の壁表現を使ってファイルに書き出す。迷路の視覚的表現も提供し、生成ロジックを後で再利用できるようにコードを整理すること。

### IV.2 使用方法

プログラムは以下のコマンドで実行する：

```
python3 a_maze_ing.py config.txt
```

- `a_maze_ing.py` はメインプログラムファイルである。この名前を使用すること。
- `config.txt` は唯一の引数であり、迷路生成オプションを定義するプレーンテキストファイルである。別のファイル名を使用してもよい。

プログラムはすべてのエラーを適切に処理すること：無効な設定、ファイルが見つからない、不正な迷路パラメータなど。予期しないクラッシュは許されず、常にユーザーに明確なエラーメッセージを提供すること。

### IV.3 設定ファイルのフォーマット

設定ファイルには1行に1つの `KEY=VALUE` ペアを含める。

`#` で始まる行はコメントであり、無視すること。

以下のキーは**必須**である：

| キー | 説明 | 例 |
|------|------|------|
| `WIDTH` | 迷路の幅（セル数） | `WIDTH=20` |
| `HEIGHT` | 迷路の高さ | `HEIGHT=15` |
| `ENTRY` | 入口座標 (x,y) | `ENTRY=0,0` |
| `EXIT` | 出口座標 (x,y) | `EXIT=19,14` |
| `OUTPUT_FILE` | 出力ファイル名 | `OUTPUT_FILE=maze.txt` |
| `PERFECT` | 迷路は完全か？ | `PERFECT=True` |

有用であれば追加のキー（例：seed、algorithm、display mode）を追加してもよい。

デフォルトの設定ファイルがGitリポジトリに含まれていること。

### IV.4 迷路の要件

- 迷路はランダムに生成されるが、シードによる再現性が必要である。
- 迷路の各セルは0〜4つの壁を持ち、各方位（北、東、南、西）に配置される。
- 迷路は有効でなければならない：
  - 入口と出口が存在し、異なる位置であり、迷路の範囲内にあること。
  - 構造が完全な接続性を保証し、孤立したセルがないこと（「42」パターンを除く。下記参照）。
  - 入口と出口は特定のセルであるため、外側の境界には壁が必要である。
  - 生成データの一貫性：隣接セルは同じ壁を共有する必要がある。例えば、あるセルが東側に壁を持ち、その背後の2番目のセルが西側に壁を持たないことは禁止される。
- 迷路に大きな開放エリアがあってはならない。通路は2セルより広くできない。例えば、2x3や3x2の開放エリアは可能だが、3x3の開放エリアは不可。
- 視覚的に表現された場合（下記参照）、迷路には複数の完全に閉じたセルで描かれた「42」が目に見えるように含まれなければならない。
- `PERFECT` フラグが有効な場合、迷路は入口と出口の間に**正確に1つの経路**を含む必要がある（すなわち完全迷路でなければならない）。

> **注意:** 迷路のサイズが許容しない場合（つまり小さすぎる場合）、「42」パターンは省略してもよい。その場合、コンソールにエラーメッセージを表示すること。

### IV.5 出力ファイルのフォーマット

迷路は出力ファイルにセルごとに1つの16進数で書き込む。各桁はどの壁が閉じているかをエンコードする：

| ビット | 方向 |
|--------|------|
| 0 (LSB) | 北 |
| 1 | 東 |
| 2 | 南 |
| 3 | 西 |

- 壁が閉じている場合はビットを1にセット、開いている場合は0。
  - 例：`3`（2進数 0011）は**南**と**西**の壁が開いていることを意味する。`A`（2進数 1010）は**東**と**西**の壁が閉じていることを意味する。
- セルは行ごとに格納され、1行に1行分。
- 空行の後、以下の3つの要素が3行で出力ファイルに挿入される：
  - 入口座標
  - 出口座標
  - 入口から出口までの最短有効経路（4つの文字 `N`、`E`、`S`、`W` を使用）
- すべての行は `\n` で終わる。

この出力ファイルは、対応する設定ファイルと組み合わせて、Moulinetteによって自動的にテストされる可能性がある。また、出力ファイルが一貫したデータを含んでいるかを確認するための検証スクリプトが本課題とともに提供される。

**出力ファイルの例：**

```
bash-3.2$ cat output_maze.txt
9515391539551795151151153
EBABAE812853C1412BA812812
96A8416A84545412AC4282C2A
C3A83816A9395384453A82D02
96842A852AC07AAD13A8283C2
C1296C43AAB83AA92AA8686BA
92E853968428444682AC12902
AC3814452FA83FFF82C52C42A
85684117AFC6857FAC1383D06
C53AD043AFFFFAFFF856AA8143
91441294297FAFD501142C6BA
AA912AC3843FAFFF82856D52A
842A8692A92B8517C4451552A
816AC384468285293917A9542
C416928513C443A828456C3BA
91416AA92C393A82801553AAA
A81292AA814682C6A8693C6AA
A8442C6C2C1168552C16A9542
86956951692C1455416928552
C545545456C54555545444556

1,1
19,14
SWSESWSESWSSSSEESEEENEESESEESSSEEESSSEEENNENEE
bash-3.2$
```

---

## V - 視覚的表現

プログラムは迷路を視覚的に表示する方法を提供する必要がある。以下のいずれかを使用：

- ターミナルASCIIレンダリング
- MiniLibX (MLX) ライブラリを使用したグラフィカル表示

ビジュアルは壁、入口、出口、解法経路を明確に表示すること。

ユーザーインタラクションは、少なくとも以下のタスクに対して利用可能でなければならない：

- 新しい迷路を再生成して表示する。
- 入口から出口までの有効な最短経路の表示/非表示を切り替える。
- 迷路の壁の色を変更する。
- オプション：「42」パターンを表示するための特定の色を設定する。

追加のユーザーインタラクションを加えてもよい。

---

## VI - コード再利用性の要件

迷路生成をユニークなクラス（例：`MazeGenerator`）として、将来のプロジェクトでインポートできるスタンドアロンモジュール内に実装すること。

以下について説明する短いドキュメントを提供すること：

- 基本的な例を含むジェネレーターのインスタンス化と使用方法。
- カスタムパラメータの渡し方（例：size、seed）。
- 生成された構造へのアクセスと、少なくとも1つの解法へのアクセス。

> **注意:** 迷路ジェネレーターモジュールは迷路構造へのアクセスを提供するが、出力ファイルと同じフォーマットである必要はない。

この再利用可能なモジュール全体（コードとドキュメント）は、`pip` による後日のインストールに適した単一ファイルとして利用可能でなければならない。
このパッケージは `mazegen-*` という名前で、ファイルはGitリポジトリのルートに配置すること。
Pythonパッケージの標準ビルドで生成される `.tar.gz` および `.whl` 拡張子の両方が許可される。
完全なファイル名の例：`mazegen-1.0.0-py3-none-any.whl`

Gitリポジトリにパッケージをビルドするために必要なすべての要素を提供すること。評価時に求められる内容：virtualenvまたは同等の環境内で、必要なツールをインストールし、ソースからパッケージを再ビルドする。

メインの `README.md` ファイル（再利用可能モジュールの一部ではない）にも、この短いドキュメントを含めること。

---

## VII - README の要件

`README.md` ファイルをGitリポジトリのルートに提供すること。その目的は、プロジェクトに馴染みのない人（ピア、スタッフ、リクルーターなど）が、プロジェクトの内容、実行方法、トピックに関する詳細情報の入手先を素早く理解できるようにすることである。

`README.md` には少なくとも以下を含めること：

- 最初の行はイタリック体で以下のように記載：*This project has been created as part of the 42 curriculum by \<login1\>[, \<login2\>[, \<login3\>[...]]].*
- プロジェクトを明確に紹介する「**Description**」セクション。目標と簡単な概要を含む。
- コンパイル、インストール、実行に関するすべての関連情報を含む「**Instructions**」セクション。
- トピックに関連する古典的な参考文献（ドキュメント、記事、チュートリアルなど）、およびAIがどのように使用されたか ── どのタスクに、どの部分のプロジェクトに使用したかの説明を含む「**Resources**」セクション。
- **プロジェクトに応じて追加セクションが必要になる場合がある**（例：使用例、機能リスト、技術的選択など）。

*必要な追加事項は以下に明示的にリストされる。*

- 設定ファイルの完全な構造とフォーマット。
- 選択した迷路生成アルゴリズム。
- そのアルゴリズムを選んだ理由。
- コードのどの部分が再利用可能で、どのように再利用可能か。
- チームとプロジェクト管理：
  - 各チームメンバーの役割。
  - 予定していた計画と最終的にどのように変化したか。
  - うまくいったことと改善できること。
  - 使用した特定のツールがあるか？どのツールか？

高度な機能を実装した場合（複数のアルゴリズム、表示オプションなど）は、この `README.md` ファイルに記述すること。

> **注意:** 英語が推奨される。あるいは、キャンパスの主要言語を使用してもよい。

---

## VIII - ボーナス

プロジェクトにさまざまなボーナスを追加してよい。以下は可能な例：

- 複数の迷路生成アルゴリズムのサポート。
- 迷路生成中のアニメーション。

---

## IX - 提出とピアレビュー

通常通りGitリポジトリに課題を提出すること。リポジトリ内の作業のみが防御中に評価される。ファイル名が正しいことを再確認することをためらわないこと。

評価中に、プロジェクトの簡単な**修正**が求められることがある。これは、軽微な動作変更、数行のコードの記述や書き換え、または追加が容易な機能である可能性がある。

このステップは**すべてのプロジェクトに適用されるわけではない**が、評価ガイドラインに記載されている場合は準備が必要である。

このステップは、プロジェクトの特定の部分に対する実際の理解を検証するためのものである。修正は、選択した開発環境（例：通常のセットアップ）で実行でき、数分以内に実現可能であるべきである ── 評価の一部として特定の時間枠が定義されていない限り。

例えば、関数やスクリプトへの小さな更新、表示の修正、新しい情報を格納するためのデータ構造の調整などを求められる場合がある。

詳細（範囲、対象など）は**評価ガイドライン**に明記されており、同じプロジェクトでも評価ごとに異なる場合がある。
